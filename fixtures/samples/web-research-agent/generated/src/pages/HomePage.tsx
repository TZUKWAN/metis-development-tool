/**
 * Generated by @mdt/generator — MDT page 'Home' (01a0a224-e67b-71b8-be48-78580663f964).
 * Generator-owned: edits may be overwritten on regeneration
 * (see .mdt/generator-manifest.json).
 */
import { MdtButton } from '../elements/MdtButton'
import { MdtInput } from '../elements/MdtInput'
import { generatedVariables } from '../generated-variables'
import { mdtElementStyle } from '../elements/layout'
import { resolveSource } from '../bindings'
import { storeForScope } from '../variables'
import { useAgentRun } from '../agent-client'
import { useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom'

export default function HomePage() {
  const navigate = useNavigate()
  const [values, setValues] = useState<Record<string, unknown>>({})
  const [agentOutputs, setAgentOutputs] = useState<Record<string, string>>({})
  const [capabilityOutputs, setCapabilityOutputs] = useState<Record<string, unknown>>({})
  const agent_0 = useAgentRun('01a0a224-e67b-71b7-a745-68f8f46efeb7')
  const setValue = (elementId: string, value: unknown): void => {
    setValues((previous) => ({ ...previous, [elementId]: value }))
  }
  const bindingCtx = { values, variables: generatedVariables, agentOutputs, capabilityOutputs }
  useEffect(() => {
    setAgentOutputs((previous) => ({ ...previous, '01a0a224-e67b-71b7-a745-68f8f46efeb7': agent_0.lastResponse }))
  }, [agent_0.lastResponse])
  async function run_0(): Promise<void> {
    // interaction 01a0a224-e67d-7c86-ad71-3d73e753dc6a (click)
    agent_0.send(toDisplayText(resolveSource({ "elementId": "01a0a224-e67d-7f3f-abce-9ee346124915", "type": "elementValue" }, bindingCtx)))
  }
  async function run_1(): Promise<void> {
    // interaction 01a0a224-e67d-7c87-a2c4-11a2efb3ca9c (click)
    navigate('/results')
  }
  function toDisplayText(value: unknown): string {
    if (value === null || value === undefined) return ''
    if (typeof value === 'string') return value
    return JSON.stringify(value, null, 2)
  }
  return (
    <div className="mdt-page" data-mdt-page='Home' data-mdt-id='01a0a224-e67b-71b8-be48-78580663f964' style={{ width: 1440, height: 1024 }}>
      <div
        data-mdt-id='01a0a224-e67c-7fff-bcd1-bfbeeb574620'
        style={mdtElementStyle({ "height": 44, "width": 520, "x": 120, "y": 60 }, { "font-size": 32 })}
      >
        {''}
      </div>
      <MdtInput
        mdtId='01a0a224-e67d-7f3f-abce-9ee346124915'
        geometry={{ "height": 44, "width": 520, "x": 120, "y": 160 }}
        value={String(values['01a0a224-e67d-7f3f-abce-9ee346124915'] ?? "")}
        placeholder='What do you want to research?'
        label='Query'
        onChange={(value) => setValue('01a0a224-e67d-7f3f-abce-9ee346124915', value)}
      />
      <MdtButton
        mdtId='01a0a224-e67d-7c83-a02e-e4d8e9a50929'
        geometry={{ "height": 44, "width": 140, "x": 660, "y": 160 }}
        label='Search'
        onClick={() => void run_0()}
        onClick={() => void run_1()}
      />
    </div>
  )
}
