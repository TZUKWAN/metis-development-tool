/**
 * Generated by @mdt/generator — MDT page 'Results' (a0000000-0000-7000-8000-000000000009).
 * Generator-owned: edits may be overwritten on regeneration
 * (see .mdt/generator-manifest.json).
 */
import { MdtList } from '../elements/MdtList'
import { generatedVariables } from '../generated-variables'
import { resolveSource } from '../bindings'
import { storeForScope } from '../variables'
import { toArray } from '../bindings'
import { useAgentRun } from '../agent-client'
import { useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom'

export default function ResultsPage() {
  const navigate = useNavigate()
  const [values, setValues] = useState<Record<string, unknown>>({})
  const [agentOutputs, setAgentOutputs] = useState<Record<string, string>>({})
  const [capabilityOutputs, setCapabilityOutputs] = useState<Record<string, unknown>>({})
  const agent_0 = useAgentRun('a0000000-0000-7000-8000-000000000004')
  const setValue = (elementId: string, value: unknown): void => {
    setValues((previous) => ({ ...previous, [elementId]: value }))
  }
  const bindingCtx = { values, variables: generatedVariables, agentOutputs, capabilityOutputs }
  useEffect(() => {
    setAgentOutputs((previous) => ({ ...previous, 'a0000000-0000-7000-8000-000000000004': agent_0.lastResponse }))
  }, [agent_0.lastResponse])
  useEffect(() => {
    // bindOutput a0000000-0000-7000-8000-000000000013 → element a0000000-0000-7000-8000-000000000010 (items)
    setValue('a0000000-0000-7000-8000-000000000010', agentOutputs['a0000000-0000-7000-8000-000000000004'] ?? '')
  }, [agentOutputs])
  return (
    <div className="mdt-page" data-mdt-page='Results' data-mdt-id='a0000000-0000-7000-8000-000000000009' style={{ width: 1440, height: 1024 }}>
      <MdtList
        mdtId='a0000000-0000-7000-8000-000000000010'
        geometry={{ "height": 44, "width": 520, "x": 120, "y": 80 }}
        items={[]}
        label='Findings'
      />
    </div>
  )
}
