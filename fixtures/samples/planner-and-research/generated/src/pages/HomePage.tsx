/**
 * Generated by @mdt/generator — MDT page 'Home' (a0000000-0000-7000-8000-000000000025).
 * Generator-owned: edits may be overwritten on regeneration
 * (see .mdt/generator-manifest.json).
 */
import { MdtButton } from '../elements/MdtButton'
import { MdtChat } from '../elements/MdtChat'
import { MdtInput } from '../elements/MdtInput'
import { generatedVariables } from '../generated-variables'
import { mdtElementStyle } from '../elements/layout'
import { resolveSource } from '../bindings'
import { storeForScope } from '../variables'
import { useAgentRun } from '../agent-client'
import { useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom'

export default function HomePage() {
  const navigate = useNavigate()
  const [values, setValues] = useState<Record<string, unknown>>({})
  const [agentOutputs, setAgentOutputs] = useState<Record<string, string>>({})
  const [capabilityOutputs, setCapabilityOutputs] = useState<Record<string, unknown>>({})
  const agent_0 = useAgentRun('a0000000-0000-7000-8000-000000000022')
  const setValue = (elementId: string, value: unknown): void => {
    setValues((previous) => ({ ...previous, [elementId]: value }))
  }
  const bindingCtx = { values, variables: generatedVariables, agentOutputs, capabilityOutputs }
  useEffect(() => {
    setAgentOutputs((previous) => ({ ...previous, 'a0000000-0000-7000-8000-000000000022': agent_0.lastResponse }))
  }, [agent_0.lastResponse])
  async function run_0(): Promise<void> {
    // interaction a0000000-0000-7000-8000-000000000032 (click)
    agent_0.send(toDisplayText(resolveSource({ "elementId": "a0000000-0000-7000-8000-000000000027", "type": "elementValue" }, bindingCtx)))
  }
  async function run_1(): Promise<void> {
    // interaction a0000000-0000-7000-8000-000000000033 (click)
    navigate('/research')
  }
  function toDisplayText(value: unknown): string {
    if (value === null || value === undefined) return ''
    if (typeof value === 'string') return value
    return JSON.stringify(value, null, 2)
  }
  return (
    <div className="mdt-page" data-mdt-page='Home' data-mdt-id='a0000000-0000-7000-8000-000000000025' style={{ width: 1440, height: 1024 }}>
      <div
        data-mdt-id='a0000000-0000-7000-8000-000000000026'
        style={mdtElementStyle({ "height": 44, "width": 520, "x": 120, "y": 60 }, { "font-size": 32 })}
      >
        {''}
      </div>
      <MdtInput
        mdtId='a0000000-0000-7000-8000-000000000027'
        geometry={{ "height": 44, "width": 520, "x": 120, "y": 160 }}
        value={String(values['a0000000-0000-7000-8000-000000000027'] ?? "")}
        placeholder='What should we plan and research?'
        label='Request'
        onChange={(value) => setValue('a0000000-0000-7000-8000-000000000027', value)}
      />
      <MdtButton
        mdtId='a0000000-0000-7000-8000-000000000028'
        geometry={{ "height": 44, "width": 140, "x": 660, "y": 160 }}
        label='Plan'
        onClick={() => void run_0()}
        onClick={() => void run_1()}
      />
      <MdtChat
        mdtId='a0000000-0000-7000-8000-000000000029'
        geometry={{ "height": 44, "width": 520, "x": 120, "y": 240 }}
        label='Planner'
      />
    </div>
  )
}
