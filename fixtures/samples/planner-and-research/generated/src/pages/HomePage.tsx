/**
 * Generated by @mdt/generator — MDT page 'Home' (01a0a224-e6e5-74aa-838d-063ce35808d5).
 * Generator-owned: edits may be overwritten on regeneration
 * (see .mdt/generator-manifest.json).
 */
import { MdtButton } from '../elements/MdtButton'
import { MdtChat } from '../elements/MdtChat'
import { MdtInput } from '../elements/MdtInput'
import { generatedVariables } from '../generated-variables'
import { mdtElementStyle } from '../elements/layout'
import { resolveSource } from '../bindings'
import { storeForScope } from '../variables'
import { useAgentRun } from '../agent-client'
import { useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom'

export default function HomePage() {
  const navigate = useNavigate()
  const [values, setValues] = useState<Record<string, unknown>>({})
  const [agentOutputs, setAgentOutputs] = useState<Record<string, string>>({})
  const [capabilityOutputs, setCapabilityOutputs] = useState<Record<string, unknown>>({})
  const agent_0 = useAgentRun('01a0a224-e6e5-74a7-bd70-c43b6d7dbd01')
  const setValue = (elementId: string, value: unknown): void => {
    setValues((previous) => ({ ...previous, [elementId]: value }))
  }
  const bindingCtx = { values, variables: generatedVariables, agentOutputs, capabilityOutputs }
  useEffect(() => {
    setAgentOutputs((previous) => ({ ...previous, '01a0a224-e6e5-74a7-bd70-c43b6d7dbd01': agent_0.lastResponse }))
  }, [agent_0.lastResponse])
  async function run_0(): Promise<void> {
    // interaction 01a0a224-e6e5-74b1-9016-c1aa2a8a1047 (click)
    agent_0.send(toDisplayText(resolveSource({ "elementId": "01a0a224-e6e5-74ac-b21e-624649224245", "type": "elementValue" }, bindingCtx)))
  }
  async function run_1(): Promise<void> {
    // interaction 01a0a224-e6e5-74b2-9f5b-123079dcdf43 (click)
    navigate('/research')
  }
  function toDisplayText(value: unknown): string {
    if (value === null || value === undefined) return ''
    if (typeof value === 'string') return value
    return JSON.stringify(value, null, 2)
  }
  return (
    <div className="mdt-page" data-mdt-page='Home' data-mdt-id='01a0a224-e6e5-74aa-838d-063ce35808d5' style={{ width: 1440, height: 1024 }}>
      <div
        data-mdt-id='01a0a224-e6e5-74ab-88fd-eaf2f6e3d16d'
        style={mdtElementStyle({ "height": 44, "width": 520, "x": 120, "y": 60 }, { "font-size": 32 })}
      >
        {''}
      </div>
      <MdtInput
        mdtId='01a0a224-e6e5-74ac-b21e-624649224245'
        geometry={{ "height": 44, "width": 520, "x": 120, "y": 160 }}
        value={String(values['01a0a224-e6e5-74ac-b21e-624649224245'] ?? "")}
        placeholder='What should we plan and research?'
        label='Request'
        onChange={(value) => setValue('01a0a224-e6e5-74ac-b21e-624649224245', value)}
      />
      <MdtButton
        mdtId='01a0a224-e6e5-74ad-9dc8-3e06a8348d30'
        geometry={{ "height": 44, "width": 140, "x": 660, "y": 160 }}
        label='Plan'
        onClick={() => void run_0()}
        onClick={() => void run_1()}
      />
      <MdtChat
        mdtId='01a0a224-e6e5-74ae-9d7e-d4951635d01d'
        geometry={{ "height": 44, "width": 520, "x": 120, "y": 240 }}
        label='Planner'
      />
    </div>
  )
}
